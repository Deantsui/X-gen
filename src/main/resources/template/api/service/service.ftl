package cn.dreamcatchers.xframe.serve.service;

import ${root.packageName}.${root.name};

import java.util.List;

/**
 * ${root.nameZh}
 */
public interface ${root.name}Service {

    ${root.name} save(${root.name} item);
    ${root.name} update(${root.name} item);
    void delete(List<Long> ids);
}
